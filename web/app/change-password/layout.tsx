export const metadata = {
  title: "Change password | CRM Dashboard",
  description: "Change your password"
}

export default function ChangePasswordLayout({ children }: { children: React.ReactNode }) {
  return children
}
